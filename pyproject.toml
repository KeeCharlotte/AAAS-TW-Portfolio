[project]
name = "veritrail-portfolio"
version = "0.3.0"
description = "Veritrail accounting policy implementation for portfolio evaluation"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
