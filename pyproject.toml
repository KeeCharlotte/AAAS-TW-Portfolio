[project]
name = "aaas-tw-portfolio"
version = "0.1.0"
description = "Selected accounting policy implementation for portfolio evaluation"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
